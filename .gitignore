node_modules/
playwright-report/
test-results/
.DS_Store
*.log

# Internal working documents kept locally, not published.
# The code review is superseded QA (all P0-P2 findings are fixed and the fixes are in
# the commit record); the raw model runs and adjudications contain citations that were
# never verified, so publishing them would present unchecked claims as project research.
REVIEW-2026-07-19.md
research/runs/
research/adjudications/
research/ADJUDICATION-2026-07-19.md
